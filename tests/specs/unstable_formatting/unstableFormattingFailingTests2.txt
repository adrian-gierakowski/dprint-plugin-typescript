~~ lineWidth: 40, indentWidth: 2, memberExpression.linePerExpression: true ~~
== OK ==
const k = () => function() {
  f(aaaaaaaaaaaaaaaaaaa, bbbbbbbbb => aa
    .b.c);
  Set.add(Entity.make(x));
  Set.add(Entity.make(x));
};

[expect]
const k = () =>
  function() {
    f(
      aaaaaaaaaaaaaaaaaaa,
      bbbbbbbbb =>
        aa
          .b
          .c,
    );
    Set.add(Entity.make(x));
    Set.add(Entity.make(x));
  };

== BROKEN ==
const g = () => function() {
  f(aaaaaaaaaaaaaaaaaaa, bbbbbbbbb => aaa
    .b.c);
  Set.add(Entity.make(x));
  Set.add(Entity.make(x));
};

[expect]
const g = () =>
  function() {
    f(
      aaaaaaaaaaaaaaaaaaa,
      bbbbbbbbb =>
        aaa
          .b
          .c,
    );
    Set.add(Entity.make(x));
    Set.add(Entity.make(x));
  };
