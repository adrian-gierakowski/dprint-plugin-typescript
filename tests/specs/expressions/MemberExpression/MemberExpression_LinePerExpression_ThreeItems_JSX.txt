~~ memberExpression.linePerExpression: true, lineWidth: 40, indentWidth: 4 ~~
== jsx ==
// 1. 3 elements, single line (fits)
(<div attr={obj.prop1.prop2} />);

// 2. 3 elements, spans multiple lines (forced by length)
(<div attr={obj.prop1.longPropertyThatExceedsTheLineWidth} />);

// 3. 3 elements, input multiline but fits -> should respect input newlines (consistent)
(<div attr={obj
  .prop1
  .prop2} />);

// 4. 3 elements, mixed input -> should respect input newlines (consistent)
(<div attr={obj.prop1
  .prop2} />);

// 5. 3 elements, mixed input that overflows -> consistent
(<div attr={obj.prop1
  .longPropertyThatExceedsTheLineWidth} />);
[expect]
// 1. 3 elements, single line (fits)
<div attr={obj.prop1.prop2} />;

// 2. 3 elements, spans multiple lines (forced by length)
<div
    attr={obj.prop1
        .longPropertyThatExceedsTheLineWidth}
/>;

// 3. 3 elements, input multiline but fits -> should respect input newlines (consistent)
<div
    attr={obj
        .prop1
        .prop2}
/>;

// 4. 3 elements, mixed input -> should respect input newlines (consistent)
<div
    attr={obj.prop1
        .prop2}
/>;

// 5. 3 elements, mixed input that overflows -> consistent
<div
    attr={obj.prop1
        .longPropertyThatExceedsTheLineWidth}
/>;
